name = "dnd-chronicles"
compatibility_date = "2026-06-24"

# Static Astro site served via Workers static assets.
# `npm run build` outputs to ./dist (Astro default static mode).
[assets]
directory = "./dist"
not_found_handling = "404-page"
